# Runtime dependencies of certus_re. Two, both ubiquitous.
# The optical model is plain NumPy: no just-in-time compiler, no compiled extension.
numpy>=1.24
scipy>=1.10

# Running the test suite
pytest>=7.0

# Only needed by tools/build_study_volet2.py, which rebuilds the deposited study
# from the laboratory archives. Not needed to use the deposit.
openpyxl>=3.1
